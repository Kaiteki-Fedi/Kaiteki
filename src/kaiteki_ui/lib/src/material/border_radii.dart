import 'package:flutter/rendering.dart';

class BorderRadii {
  /// Border radius of 0.0dp
  static const none = BorderRadius.zero;

  /// Border radius of 4.0dp
  static const extraSmall = BorderRadius.all(Radius.circular(4.0));

  /// Border radius of 8.0dp
  static const small = BorderRadius.all(Radius.circular(8.0));

  /// Border radius of 12.0dp
  static const medium = BorderRadius.all(Radius.circular(12.0));

  /// Border radius of 16.0dp
  static const large = BorderRadius.all(Radius.circular(16.0));

  /// Border radius of 28.0dp
  static const extraLarge = BorderRadius.all(Radius.circular(28.0));
}
